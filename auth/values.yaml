# Default values for auth chart
github:
  oauthOpenShiftId: ""  # GITHUB_OAUTH_OPENSHIFT_ID
  clientSecret:
    name: github-oauth
  teams:
    # Format: <org>:<team-slug>
    # Example: etsauer-acme:admin-team
    # This enables group-based authorization via RoleBindings
    # - etsauer-acme:admin-team
